import './QuestionTestCard.css';

interface ContainerProps {
  name: string;
}

const QuestionTestCard: React.FC<ContainerProps> = ({ name }) => {
  return (
  );
};

export default QuestionTestCard;
